# `cli` module

@../docs/architecture/cli/README.md

Module-specific guidance for the `cli` module. The import above pulls in this module's architecture document. For
project-wide workflow, conventions, build, test, and coverage guidance, see the root `CLAUDE.md` at the repository
root.
