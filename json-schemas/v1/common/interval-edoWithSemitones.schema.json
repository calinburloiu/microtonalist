{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Semitones and an offset in divisions from that",
  "description": "An array with two items, where the first one is the approximate number of 12-EDO semitones and the second one is the offset from that in divisions.",
    "type": "array",
    "prefixItems": [
        {
            "title": "Semitones",
            "description": "The approximate number of 12-EDO semitones.",
            "type": "integer"
        },
        {
            "title": "Divisions",
          "description": "The offset from the number of semitones (first item) expressed in divisions of the given EDO.",
            "type": "integer"
        }
    ],
    "items": false,
    "minItems": 2
}
